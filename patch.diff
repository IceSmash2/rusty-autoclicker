diff --git a/Cargo.toml b/Cargo.toml
index 1234567..abcdef0 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -20,6 +20,8 @@ sanitizer = { version = "1.0.0", default-features = false }
 native-dialog = "0.9"
 rand = "0.10.1"
 rdev = "0.5"
+serde = { version = "1", features = ["derive"] }
+serde_json = "1"

diff --git a/src/main.rs b/src/main.rs
index 1234567..abcdef0 100644
--- a/src/main.rs
+++ b/src/main.rs
@@ -5,6 +5,7 @@ mod app;
 mod defines;
 mod gui;
 mod types;
+mod settings;
 mod utils;

diff --git a/src/settings.rs b/src/settings.rs
new file mode 100644
index 0000000..abcdef0
--- /dev/null
+++ b/src/settings.rs
@@ -0,0 +1,140 @@
+use std::fs;
+use std::io::{self, Read, Write};
+use std::path::PathBuf;
+
+use device_query::Keycode;
+use serde::{Deserialize, Serialize};
+
+use crate::defines::{
+    DEFAULT_HR_STR, DEFAULT_MIN_STR, DEFAULT_SEC_STR, DEFAULT_MS_STR,
+    DEFAULT_CLICK_AMOUNT_STR, DEFAULT_CLICK_X_STR, DEFAULT_CLICK_Y_STR,
+    MOUSE_TWEEN_SPEED_MIN_PX_S, MOUSE_TWEEN_SPEED_MAX_PX_S,
+    HOTKEY_AUTOCLICK, HOTKEY_SET_COORD, HOTKEY_HOLD,
+};
+
+#[derive(Serialize, Deserialize, Debug, Clone)]
+pub struct Settings {
+    pub hr_str: String,
+    pub min_str: String,
+    pub sec_str: String,
+    pub ms_str: String,
+
+    pub click_amount_str: String,
+    pub click_x_str: String,
+    pub click_y_str: String,
+
+    pub speed_min_str: String,
+    pub speed_max_str: String,
+
+    pub key_autoclick: Option<Keycode>,
+    pub key_set_coord: Option<Keycode>,
+    pub key_hold: Option<Keycode>,
+}
+
+impl Default for Settings {
+    fn default() -> Self {
+        Self {
+            hr_str: DEFAULT_HR_STR.to_owned(),
+            min_str: DEFAULT_MIN_STR.to_owned(),
+            sec_str: DEFAULT_SEC_STR.to_owned(),
+            ms_str: DEFAULT_MS_STR.to_owned(),
+
+            click_amount_str: DEFAULT_CLICK_AMOUNT_STR.to_owned(),
+            click_x_str: DEFAULT_CLICK_X_STR.to_owned(),
+            click_y_str: DEFAULT_CLICK_Y_STR.to_owned(),
+
+            speed_min_str: MOUSE_TWEEN_SPEED_MIN_PX_S.to_string(),
+            speed_max_str: MOUSE_TWEEN_SPEED_MAX_PX_S.to_string(),
+
+            key_autoclick: HOTKEY_AUTOCLICK,
+            key_set_coord: HOTKEY_SET_COORD,
+            key_hold: HOTKEY_HOLD,
+        }
+    }
+}
+
+fn settings_path() -> PathBuf {
+    PathBuf::from("rusty_autoclicker_settings.json")
+}
+
+pub fn load_settings() -> Settings {
+    let path = settings_path();
+    if let Ok(mut file) = fs::File::open(path) {
+        let mut buf = String::new();
+        if file.read_to_string(&mut buf).is_ok() {
+            if let Ok(settings) = serde_json::from_str::<Settings>(&buf) {
+                return settings;
+            }
+        }
+    }
+    Settings::default()
+}
+
+pub fn save_settings(settings: &Settings) -> io::Result<()> {
+    let path = settings_path();
+    let json = serde_json::to_string_pretty(settings)?;
+    let mut file = fs::File::create(path)?;
+    file.write_all(json.as_bytes())
+}

diff --git a/src/app.rs b/src/app.rs
index 1234567..abcdef0 100644
--- a/src/app.rs
+++ b/src/app.rs
@@ -1,6 +1,7 @@
 use std::time::{Duration, Instant};
 
 use device_query::{Keycode, MouseState};
+use crate::settings::{Settings, load_settings, save_settings};
 
@@ -120,6 +121,63 @@ impl RustyAutoClickerApp {
         Default::default()
     }
 
+    pub fn apply_settings(&mut self, s: Settings) {
+        self.hr_str = s.hr_str;
+        self.min_str = s.min_str;
+        self.sec_str = s.sec_str;
+        self.ms_str = s.ms_str;
+
+        self.click_amount_str = s.click_amount_str;
+        self.click_x_str = s.click_x_str;
+        self.click_y_str = s.click_y_str;
+
+        self.speed_min_str = s.speed_min_str;
+        self.speed_max_str = s.speed_max_str;
+
+        self.key_autoclick = s.key_autoclick;
+        self.key_set_coord = s.key_set_coord;
+        self.key_hold = s.key_hold;
+    }
+
+    pub fn export_settings(&self) -> Settings {
+        Settings {
+            hr_str: self.hr_str.clone(),
+            min_str: self.min_str.clone(),
+            sec_str: self.sec_str.clone(),
+            ms_str: self.ms_str.clone(),
+
+            click_amount_str: self.click_amount_str.clone(),
+            click_x_str: self.click_x_str.clone(),
+            click_y_str: self.click_y_str.clone(),
+
+            speed_min_str: self.speed_min_str.clone(),
+            speed_max_str: self.speed_max_str.clone(),
+
+            key_autoclick: self.key_autoclick,
+            key_set_coord: self.key_set_coord,
+            key_hold: self.key_hold,
+        }
+    }
+
+    pub fn capture_mouse_to_coords(&mut self) {
+        let (x, y) = self.mouse.coords;
+        self.click_x_str = x.to_string();
+        self.click_y_str = y.to_string();
+        self.click_position = ClickPosition::Coord;
+
+        let _ = save_settings(&self.export_settings());
+    }
+
@@ -140,7 +198,12 @@ impl RustyAutoClickerApp {
     pub fn new(cc: &eframe::CreationContext<'_>) -> Self {
         let ctx = &cc.egui_ctx;
 
-        Default::default()
+        let mut app = RustyAutoClickerApp::default();
+        let settings = load_settings();
+        app.apply_settings(settings);
+        app
     }
 }
 
@@ -200,6 +263,9 @@ impl Drop for RustyAutoClickerApp {
         if let Some(button) = self.held_button.take() {
             release_button(button);
         }
+
+        let _ = save_settings(&self.export_settings());
     }
 }

diff --git a/src/gui/windows.rs b/src/gui/windows.rs
index 1234567..abcdef0 100644
--- a/src/gui/windows.rs
+++ b/src/gui/windows.rs
@@ -1,6 +1,7 @@
 use eframe::egui::{self, Context};
 
 use crate::{RustyAutoClickerApp, types::InteractionMode};
+use crate::settings::save_settings;
 
@@ -20,6 +21,7 @@ impl RustyAutoClickerApp {
                         if idle {
                             self.mode = InteractionMode::SettingAutoclickKey;
                             self.key_autoclick = None;
+                            let _ = save_settings(&self.export_settings());
                         }
                     };
@@ -45,6 +47,7 @@ impl RustyAutoClickerApp {
                         if idle {
                             self.key_set_coord = None;
                             self.mode = InteractionMode::SettingSetCoordKey;
+                            let _ = save_settings(&self.export_settings());
                         }
                     };
@@ -70,6 +73,7 @@ impl RustyAutoClickerApp {
                         if idle {
                             self.key_hold = None;
                             self.mode = InteractionMode::SettingHoldKey;
+                            let _ = save_settings(&self.export_settings());
                         }
                     };

diff --git a/src/gui/sections/click_config.rs b/src/gui/sections/click_config.rs
index 1234567..abcdef0 100644
--- a/src/gui/sections/click_config.rs
+++ b/src/gui/sections/click_config.rs
@@ -1,6 +1,7 @@
 use eframe::egui::{self, Context};
 
 use crate::{
     RustyAutoClickerApp,
     types::{ClickPosition, ClickType},
 };
+use crate::settings::save_settings;
 
@@ -60,6 +61,10 @@ impl RustyAutoClickerApp {
                 ui.add(
                     egui::TextEdit::singleline(&mut self.click_y_str)
                         .desired_width(50.0f32)
                         .hint_text("0"),
                 );
+                if ui.changed() {
+                    let _ = save_settings(&self.export_settings());
+                }
+
                 ui.label("Y");
@@ -70,6 +75,10 @@ impl RustyAutoClickerApp {
                 ui.add(
                     egui::TextEdit::singleline(&mut self.click_x_str)
                         .desired_width(50.0f32)
                         .hint_text("0"),
                 );
+                if ui.changed() {
+                    let _ = save_settings(&self.export_settings());
+                }
+
                 ui.label("X");
@@ -80,6 +89,8 @@ impl RustyAutoClickerApp {
                 if ui
                     .selectable_value(&mut self.click_position, ClickPosition::Coord, "Coords")
                     .clicked()
                 {
                     self.key_pressed_set_coord = true;
+                    self.capture_mouse_to_coords();
+                    let _ = save_settings(&self.export_settings());
                 };
@@ -90,6 +101,7 @@ impl RustyAutoClickerApp {
                 if ui
                     .selectable_value(&mut self.click_position, ClickPosition::Mouse, "Mouse")
                     .clicked()
                 {
                     self.key_pressed_set_coord = false;
+                    let _ = save_settings(&self.export_settings());
                 };

